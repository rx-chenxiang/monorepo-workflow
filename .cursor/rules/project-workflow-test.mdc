---
description: 测试 / E2E / 验收流程（通用四端）
globs: tests/**,**/tests/**,**/e2e/**,docs/general/**
---

# 测试与验收 Workflow

## 当前文件说明

本规则用于通用工作区的功能测试、E2E、验收、回归、联调验证与测试报告产出。测试范围按 `api/`、`fornt_admin/`、`m_front/`、`pc_fornt/` 四端识别。

## 触发条件

用户提到 `测试`、`E2E`、`验收`、`回归`、`联调验证`、`Playwright`、`帮我验`、`测试用例`、`测试报告` 时，加载本规则。

## 测试前必读

1. 根目录 `AGENTS.md`：确认测试命中的端别。
2. `docs/README.md`：确认需求文档位置。
3. 目标子项目 `AGENTS.md` 与 `docs/README.md`：确认项目规则和回归入口。
4. 若涉及接口，读取 `api/docs/modules/README.md` 或实际接口契约文档。

## 执行流程

1. 明确测试目标、环境、账号、入口 URL、接口依赖和验收标准。
2. 按端拆分测试范围：后端接口、管理平台、门户端、官网。
3. 优先执行可自动化验证；无法自动化时记录手工验证步骤。
4. 记录通过、失败、阻塞、风险和证据路径。
5. 需要复测时，基于同一验收标准复测并说明差异。

## 自检清单

- [ ] 是否确认目标端和环境。
- [ ] 是否覆盖主流程、异常流、权限态、空态和边界值。
- [ ] 是否未使用前端 Mock 替代真实接口联调。
- [ ] 是否记录可复现步骤和证据。
- [ ] 是否说明无法验证的阻塞点。

<!-- AIGC:cursor|author:沉香|lines:约52|dates:2026-07|功能说明:测试workflow初始化为通用四端验收规则 -->

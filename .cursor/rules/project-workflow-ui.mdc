---
description: 设计 / UI / 原型流程（通用三前端）
globs: docs/general/**/需求文档/**,fornt_admin/**,m_front/**,pc_fornt/**
---

# 设计与 UI Workflow

## 当前文件说明

本规则用于通用工作区的 UI、UX、原型、交互、设计评审与前端落地衔接。设计产出应能映射到 `fornt_admin/`、`m_front/`、`pc_fornt/` 中的目标端。

## 触发条件

用户提到 `设计`、`UI`、`UX`、`原型`、`交互`、`设计稿`、`mockup`、`Figma`、`视觉`、`改版`、`体验优化` 时，加载本规则。

## 执行流程

1. 先确认目标端：管理平台、门户端、官网或多端。
2. 读取根 `AGENTS.md`、目标前端 `AGENTS.md` 与 `docs/README.md`。
3. 明确目标用户、核心任务、页面状态、响应式范围和验收标准。
4. 输出原型或设计说明时，按端说明页面结构、组件、状态和接口依赖。
5. 进入编码前，衔接 `project-workflow-front.mdc` 并读取目标模块索引。

## 设计约束

1. 管理平台优先信息密度、可扫描性和操作效率。
2. 门户端优先移动端可用性、加载状态和触控体验。
3. 官网优先首屏表达、品牌可信度、性能、SEO 和响应式体验。
4. 设计说明不得脱离实际前端技术栈和已有组件体系。

<!-- AIGC:cursor|author:沉香|lines:约42|dates:2026-07|功能说明:UI workflow初始化为通用三前端设计规则 -->

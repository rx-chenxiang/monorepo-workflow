---
description: 接口优先与禁用 Mock 数据开发规范
alwaysApply: true
---

# 接口优先开发规范

## 当前文件说明

本规则用于统一当前项目的前后端协作顺序：必须先实现并确认真实接口，再进行前端功能编码；开发与联调阶段禁止使用 Mock 数据，保障业务行为与线上一致。

## 核心约束

1. 所有需求开发必须遵循“后端接口先完成，再开始前端编码”。
2. 前端开发、联调、提测阶段均禁止新增或使用 Mock 数据（包括本地静态假数据、拦截器伪造响应、Mock 平台临时数据）。
3. 如需测试数据，必须通过数据库建立测试数据，并走真实接口进行验证。
4. 若接口未就绪或返回结构不明确，先阻塞前端实现并同步接口方，不得以 Mock 方案替代正式联调。

## 执行门禁

1. 开始前端编码前，先按根目录 `AGENTS.md` 的「子项目文档优先级」定位配套后端的 `AGENTS.md`、`docs/README.md` 与实际接口或模块文档，再确认接口文档、字段定义、状态码语义已明确可用。
2. 提交前自检：确认代码中无 Mock 开关、无本地假数据回退、无伪造响应逻辑。
3. 如发现历史 Mock 逻辑影响当前需求，应在本次改动中移除或禁用后再提测。

<!-- AIGC:codex|author:沉香|lines:约2|dates:2026-07|功能说明:接口门禁统一按子项目AGENTS与docs索引定位真实接口契约 -->
<!-- AIGC:cursor|author:沉香|lines:24(说明:本次新增代码行数)|dates:2026-05 功能说明:新增接口优先且禁用Mock数据的全局开发规则 -->

---
description: 产品 / PRD / 需求规划流程（通用 general 项目组）
globs: docs/general/**,docs/_template/**
---

# 产品与需求 Workflow

## 当前文件说明

本规则用于通用工作区的 PRD、需求说明、技术规划、验收标准、编码计划等文档产出。需求级文档统一落盘到 `docs/general/{需求名称}/`。

## 触发条件

用户提到 `产品`、`PRD`、`需求文档`、`需求说明`、`整理需求`、`写需求`、`技术规划`、`模块规划`、`需求转文档`、`编码前文档`、`业务方案`、`功能范围`、`排期`、`优先级`、`验收标准`、`用户故事`、`@docs/general` 时，加载本规则。

## 目录约定

```text
docs/general/{需求名称}/
├── 需求文档/
├── 技术设计方案/
└── coding-plan/
```

## 执行流程

1. 先确认需求涉及端别：`api`、`fornt_admin`、`m_front`、`pc_fornt` 或全部四端。
2. 读取根 `AGENTS.md`、`docs/README.md` 与相关子项目 `docs/README.md`。
3. 将用户原始描述整理为目标、范围、角色、流程、接口、数据、状态、异常、验收标准。
4. 涉及技术设计时，明确各端职责、接口契约、状态流、风险点和验证方式。
5. 文档落盘到 `docs/general/{需求名称}/` 对应子目录。
6. 输出结果时说明文档路径和后续编码建议。

## 文档要求

1. 需求边界清晰，明确不做什么。
2. 多端需求必须按端拆分职责。
3. 接口需求必须落到可联调的字段、状态码、错误码、鉴权和幂等约束。
4. 验收标准必须可测试、可复现、可判断通过或失败。

<!-- AIGC:cursor|author:沉香|lines:约48|dates:2026-07|功能说明:产品workflow初始化为通用general需求目录规则 -->

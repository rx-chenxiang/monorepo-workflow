# 模块技术规划文档骨架

> 复制本骨架撰写 `docs/bss/{需求名称}/技术设计方案/{序号}-{模块}模块技术规划.md`。  
> 完整示例见 `02-题库模块技术规划.md`、`01-首页模块技术规划.md`。

```markdown
---
创建时间：YYYY-MM-DD
功能模块：产品 - {模块中文名}
规划状态：待实现
关联设计图：
  - docs/bss/{需求名称}/需求文档/设计图/{模块}/xxx.jpg
---

# {模块中文名} 技术规划文档

> AIGC:cursor|author:沉香|lines:N|dates:YYYY-MM {模块}-技术规划

## 1. 模块概述

### 1.1 功能定位
（2～4 句：模块在 APP/后台中的角色与业务价值）

### 1.2 用户故事
| ID | 角色 | 场景 | 期望 |
|----|------|------|------|
| US-01 | … | … | … |

### 1.3 核心功能列表
1. **功能 A**：…
2. **功能 B**：…

---

## 2. 页面结构与路由

### 2.1 页面列表（bss_front）
| 页面名 | 路由 path（`/home` 子路由） | 说明 |
|--------|--------------------------|------|
| … | `/home/xxx` | 见 `bss_front/src/router/index.js` |

### 2.2 页面导航流程
\`\`\`
入口页 (pages/...)
  ├─ 操作 A → 目标页
  └─ 操作 B → 目标页
\`\`\`

（可选 mermaid 流程图）

---

## 3. UI 组件规划（bss_front）

> 遵循 `docs/bss_front/架构说明.md`；复用 `table.vue`、`cus_dialog.vue` 等公共组件。

### 3.1 页面级组件
| 区块 | 组件名 | Element Plus / 样式 | 备注 |
|------|--------|---------------------|------|
| … | `XxxTable` | — | … |

### 3.2 公共组件复用
- 复用：`{frontRoot}` 下现有公共组件（以命中子项目 docs/README 与代码为准）
- 新增：业务目录下局部组件

### 3.3 关键组件说明（可选）
（仅对复杂交互组件写结构说明或伪代码，避免全文贴 Vue）

---

## 4. 数据模型设计

### 4.1 数据库实体
> 后端：`{代码目录}` 下目标 Domain Models，文档为注册表命中子项目 `docs/modules/README.md` 实际链接的模块文档 §3

#### `{table_name}`（表中文名）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | - |
| … | … | … |

**索引**：`idx_xxx`

### 4.2 实体关系（ER）
\`\`\`
entity_a 1—N entity_b
\`\`\`

---

## 5. API 接口规划

### 5.1 内部管理 API（`routes/webApi.php`，前缀 `/web_api/`）
| 接口名 | Method | 路径 | Controller | 说明 |
|--------|--------|------|------------|------|
| … | POST | `/web_api/...` | `Web\...` | AES + JWT |

### 5.2 签名 API（`routes/api.php`）
| 接口名 | Method | 路径 | 说明 |
|--------|--------|------|------|
| … | POST | `/xxx/yyy` | VerifyApiSign |

### 5.3 核心接口请求/响应示例
#### 5.3.1 {接口名}
**请求**：`POST /web_api/...`
\`\`\`json
{ }
\`\`\`
**响应**：
\`\`\`json
{ "code": 1000, "data": {} }
\`\`\`

---

## 6. 状态管理（bss_front，可选）

### 6.1 Pinia / Vuex
| Store | 职责 |
|-------|------|
| … | … |

---

## 7. 后台管理功能（bss_front）

| 页面 | views 目录 | api.js 函数 | 说明 |
|------|-----------|-------------|------|
| … | `src/views/...` | `xxxApi` | … |

---

## 8. 开发注意事项

1. **鉴权**：webApi JWT；禁止前端 Mock（见 `api-first-no-mock`）
2. **分页**：与现有接口字段一致
3. **加密**：请求经 `http.js` AES + safetyToken
4. **其他**：…

---

## 9. 开发里程碑

| 模块 | 子模块 | 优先级 | 预估工时 | 依赖 |
|------|--------|--------|---------|------|
| 后端 Service/Controller | … | P0 | 1d | - |
| bss_front 页面/API | … | P0 | 2d | 后端接口就绪 |

---

## 10. 风险与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| … | … | … |

---

## 11. 成功标准

- [ ] 主流程闭环可演示（具体路径）
- [ ] 核心接口联调通过
- [ ] 后台可配置/运营必要数据
- [ ] …

---

## 变更记录（更新文档时追加）

| 日期 | 变更说明 | 作者 |
|------|---------|------|
| YYYY-MM-DD | 初稿 | 沉香 |
```

<!-- AIGC:cursor|author:沉香|lines:约130|dates:2026-05|功能说明:模块技术规划 Markdown 骨架模板 -->

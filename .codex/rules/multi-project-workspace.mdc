---
description: 通用项目工作区多端协作规则（api + fornt_admin + m_front + pc_fornt）
alwaysApply: true
---

# 通用项目工作区 · 多端协作规范

## 当前文件说明

本规则用于约束 **通用项目工作区** 内四端（后端 API + 管理平台前端 + 门户端前端 + 官网前端）并行开发时的执行边界、任务拆分和交付方式，避免跨端误改与上下文混用。**项目组 / 子项目 / 代码目录以 `AGENTS.md` 注册表为唯一事实来源**，本规则不写死业务模块名。工作区总说明见 `README.md`。

**口语路由（先于本规则）**：用户说通用 / 全栈 / 全部 / 四端 → 范围 = general 四端，见 `.codex/rules/project-routing.mdc` 与 `AGENTS.md`（项目注册表一节）。

## 适用范围

1. 当前会话涉及单端开发时，按目标端规则执行，禁止主动触达其他端。
2. 当前会话涉及多端联动时，按「端维度拆分」执行，逐项完成并回报。
3. 根目录规则仅做流程兜底；子项目细则以各自版本化 `AGENTS.md`、`docs/README.md` 与实际 `docs/` 索引为准。

## 项目目录边界

按子项目端别套用 workflow（`{代码目录}` 由 `AGENTS.md` 注册表解析）：

| 端别 | 代码目录 | 工作流规则 | 模块文档 |
|------|---------|-----------|---------|
| 后端 API | `api/**` | `.codex/rules/project-workflow-api.mdc` | `api/docs/README.md` 与实际模块索引 |
| 管理平台前端 | `fornt_admin/**` | `.codex/rules/project-workflow-front.mdc` | `fornt_admin/docs/README.md` 与实际模块索引 |
| 门户端前端 | `m_front/**` | `.codex/rules/project-workflow-front.mdc` | `m_front/docs/README.md` 与实际模块索引 |
| 官网前端 | `pc_fornt/**` | `.codex/rules/project-workflow-front.mdc` | `pc_fornt/docs/README.md` 与实际模块索引 |

## 子项目入口文档

识别 `{代码目录}` 后按下列模式 Read（各子项目实际文件以其目录为准）：

| 用途 | 路径模式 |
|------|---------|
| 子项目入口 | `{代码目录}AGENTS.md`（若有）→ `{代码目录}docs/README.md`（若有） |
| 工程规则与模块定位 | `{代码目录}docs/rules/`、`{代码目录}docs/modules/README.md`（以实际存在为准） |
| 复杂业务与 workflow | `{代码目录}docs/codebase/`、`{代码目录}docs/skills/`（以实际存在为准） |

## 执行流程（必须遵守）

1. 先识别本次需求涉及的目标端（后端、管理平台、门户端、官网或多端）。
2. 若需求描述不清晰，先确认目标端后再动手。
3. **编码前**：各端分别按根 `AGENTS.md`「子项目文档优先级」加载上下文，再 Read 对应 workflow。
4. 按端逐一实现：先分析范围，再最小改动实现，再基础自检。
5. **编码后**：由开发 Agent 或 `doc-updater` 同步目标子项目索引中实际存在的模块、专题或架构文档。
6. 输出结果时按端分组说明「改了什么、为什么改、如何验证」。

## 前后端联调约定

| 维度 | 约定 |
|------|------|
| 接口前缀 | 以 `api` 实际路由与接口契约为准 |
| 请求安全 | 以 `api/docs/codebase/` 中实际鉴权、签名、Token 文档为准 |
| 接口契约 | 以 Apifox / OpenAPI / 后端实际模块或 API 文档为准，**禁止**前端 mock |
| 模块对照 | 前端模块名 ↔ 后端领域模块，按各端 `docs/modules/README.md` 维护 |
| 开发代理 | 前端构建配置中将接口前缀 proxy 到对应后端环境 |

## 最小改动与安全约束

1. 遵循 KISS 与 SOLID，避免引入不必要抽象。
2. 优先修改目标模块/业务域，避免修改全局配置和公共底层。
3. 未经明确要求，不进行跨端重构与大范围风格调整。
4. 涉及多端联调时，优先保证接口契约稳定，避免破坏现有行为。
5. 后端：不主动改全局中间件链、数据库连接、鉴权链路，除非需求明确要求。
6. 前端：不主动改全局请求封装、登录态、权限路由，除非需求明确要求。

## 文档同步（开发闭环）

编码完成后须同步模块文档（详见 `doc-updater` Agent）：

| 变更项目 | 更新文档 |
|---------|---------|
| 后端 Service/Controller/Model | `api/docs/modules/README.md` 索引定位的实际文档；无模块文档时更新相关专题或 README 索引 |
| 前端 views/API/components | 对应前端子项目模块或架构索引定位的实际文档 |
| 架构级变更 | 子项目架构说明或 `{代码目录}docs/codebase/` 专题 |
| 新增业务域/页面模块 | 模块文档 + README 索引 + workflow 映射表 |

## 关联 Agent

| Agent | 用途 |
|-------|------|
| `api-developer` | 后端 API 开发 + 文档同步（按 AGENTS.md 识别子项目） |
| `front-developer` | 前端页面开发 + 文档同步（按 AGENTS.md 识别子项目） |
| `doc-updater` | 开发闭环末尾文档批量同步 |

## 交付要求

1. 每个子项目的变更都应包含「当前文件说明」与必要中文注释。
2. 对新增或更新规则文件，补充 AIGC 注释便于追溯。
3. 全栈变更交付时，前后端实际文档中的接口/API 信息须互相可对照。
4. 如无法完成验证，明确说明阻塞点与建议下一步。

<!-- AIGC:cursor|author:沉香|lines:约105|dates:2026-07|功能说明:多端协作规则初始化为通用四端版本，固定api、fornt_admin、m_front、pc_fornt边界 -->
